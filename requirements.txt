# 런타임 의존성 — 2026-08-07 PyPI 최신 릴리스로 전량 고정(==).
# 검증: Python 3.14.3 클린 venv에서 resolve 및 `pytest` 통과 확인.
# 갱신 방법은 README "의존성 버전 정책" 참고.

# --- 웹 프레임워크 ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-dotenv==1.2.2
pydantic==2.13.4
PyYAML==6.0.3

# --- DB ---
SQLAlchemy==2.0.51
alembic==1.19.0
psycopg[binary]==3.3.4

# --- LLM ---
openai==2.53.0
# 채팅은 LangChain Google provider, 감정 분석은 Google SDK를 직접 사용한다.
langchain-core==1.5.3
langchain-google-genai==4.3.2
google-genai==1.75.0

# --- 미사용 (제거 후보) ---
# 아래 4개는 app/·tests/ 어디에서도 import 되지 않는다(2026-08-07 grep 확인).
# 유지 시 langgraph·numpy·tokenizers·huggingface_hub·aiohttp 등이 함께 설치되어
# 설치 패키지가 57개 → 84개로 늘어난다. 제거 판단 전까지 버전만 고정해 둔다.
langchain==1.3.14
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
# python-multipart: UploadFile/Form을 쓰는 엔드포인트가 없어 현재 불필요.
python-multipart==0.0.32
